from django.apps import AppConfig                                          # Clase base para configurar aplicaciones de Django


class SocialConfig(AppConfig):                                             # Configuración de la app 'social'
    name = 'social'                                                        # Nombre de la app, debe coincidir con el directorio