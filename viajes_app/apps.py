from django.apps import AppConfig  # Importa la clase base para configurar la app


class ViajesAppConfig(AppConfig):  # Configuración de la aplicación viajes_app
    name = 'viajes_app'  # Nombre interno de la app (debe coincidir con el folder)

   