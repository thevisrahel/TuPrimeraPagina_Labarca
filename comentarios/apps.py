from django.apps import AppConfig                    # Importa la clase base para configurar apps en Django


class ComentariosConfig(AppConfig):                  # Configuración de la app 'comentarios'

    name = 'comentarios'                             # Nombre interno de la app (debe coincidir con la carpeta)